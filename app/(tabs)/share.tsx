import React from 'react';
import ShareScreen from '../share';

// Wrapper simple pour exposer la page de partage dans le groupe (tabs)
export default function ShareTab() {
  return <ShareScreen />;
}
